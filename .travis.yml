os:
  - linux
language: cpp
compiler:
  - gcc
  - clang
install:
  - sudo apt-get install libprotobuf-dev protobuf-compiler
script: ./autogen.sh && ./configure && make -j 2
branches:
  only:
    - master
    - develop
notifications:
  recipients:
    - tommy@monetas.net
  email:
    on_success: change
    on_failure: always
